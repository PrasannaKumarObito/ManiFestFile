apiVersion: v1
kind: Pod
metadata:
  name: config-map
spec:
  containers:
  - name: config-map
    image: nginx
    ports:
      - containerPort: 80
    env:
      - name: Configmap-pod-1
        valumeFrom:
          configMapKeyRef:
            name: cm
            key: APP_ENV
      - name: Configmap-pod-2
        valumeFrom:
          configMapKeyRef:
            name: config-map
            key: Cicd 
      - name: Configmap-pod-3
        valumeFrom:
          configMapKeyRef:
            name: cm3
            key: env